{
  "rules": {

    "users": {
      ".indexOn": ["phoneNumber", "lastActive"],
      "$uid": {
        "phoneNumber": {
          ".read": true,
          ".write": false
        },
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
